# IDE
.idea/

# Root-package.json ist nur eine Skript-Durchreiche nach frontend/ —
# ein `npm install` hier oben soll nichts hinterlassen.
/node_modules
/package-lock.json

# local env / secrets (frontend/.gitignore covers *.local too — belt and braces)
*.local
.env

# wrangler-Cache — der launch.json-Eintrag „geo-quiz-pages" startet
# `wrangler pages dev` aus dem Repo-Root und legt ihn hier an.
/.wrangler

# OS
Thumbs.db
.DS_Store

# Claude Code runtime state (lokal, nicht versionieren)
.claude/settings.local.json
.claude/scheduled_tasks.lock
